# heading-test
Heading, turns sensor based testing no 1

- This is initial test for finding turns based on iOS sensors (once this becomes stable version, same has to interact alongside speedometer to determine the headings)

- headings + turns + offline map with long & lat = hop longitude/latitude based on points over map grid to find device movements
